import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('TripSplit', () => App);
AppRegistry.runApplication('TripSplit', {
  rootTag: document.getElementById('root'),
});
